import Head from 'next/head';
import styled from 'styled-components';

const Wrapper = styled.div(() => ({
  width: '100%',
  maxWidth: '620px',
  padding: '0 20px',
  margin: '0 auto',
}));

const Layout: React.FC = ({ children }) => (
  <Wrapper>
    <Head>
      <link rel='preload' href='/fonts/Vremena/Book.woff' as='font' type='font/woff2' crossOrigin='' />
      <link rel='preload' href='/fonts/Vremena/Medium.woff' as='font' type='font/woff2' crossOrigin='' />
      <title>Bartłomiej Jacak — Digital Creator</title>
      {/* <meta name='description' content='Generated by create next app' /> */}
      {/* <link rel='icon' href='/favicon.ico' /> */}
    </Head>
    {children}
  </Wrapper>
);

export default Layout;
